# Same Supabase project as the web app's .env.local — see the repo root's
# .env.local.example for where these values come from. Both are public
# (anon/publishable) keys, safe to expose in a client bundle.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
